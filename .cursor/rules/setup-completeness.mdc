---
description: 需要使用者到外部後台（LINE、Railway、GitHub 等）點設定時，必須一次列出全部必要項目
alwaysApply: true
---

# 外部設定一次講完

本專案大量依賴外部後台設定（LINE Developers、LINE Official Account Manager、Railway）。分批擠牙膏式的指示會浪費使用者大量時間。

## 規則

1. 任何需要使用者到外部後台操作的回覆，**必須一次列出該階段所有必要設定**，不得只講當下卡住的那一項。
2. 每項設定要寫：**位置路徑 → 要求值 → 漏掉會出現什麼錯誤**。
3. 先查 `docs/setup-checklist.md`；該檔是設定的權威來源。有新發現的設定項或錯誤成因，**同一輪**補進去。
4. 能用 API／CLI 查證的實際值（LIFF scope、webhook endpoint、Railway 變數是否存在），先查證再說，不要用猜測要求使用者反覆試。
5. 診斷外部錯誤時，優先讓程式印出**失敗階段與原始錯誤碼**，而不是給模糊的萬用建議。
6. 提到金鑰時只寫變數名稱與取得位置，永不輸出值；也不要求使用者把值貼進對話。
